# Dagger CI Modules

Python-based Dagger modules for the CI pipeline.

- `book.py` — Build the AsciiDoc book to HTML and PDF
- `test.py` — Run code examples per chapter
- `lint.py` — Multi-language linting
- `diagrams.py` — Render diagram scripts via uv
- `helm_test.py` — K8s integration tests
